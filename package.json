{
  "name": "arc-plane",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-plane": "tsc && node dist/plane.js",
    "start-ground-station": "tsc && node dist/ground-station.js",
    "start-plane-old": "tsc -p tsconfig-plane.json && node dist/plane/plane.js",
    "start-ground-station-old": "tsc -p tsconfig-ground-station.json && node dist/ground-station/ground-station.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.11.2",
    "tslint": "^6.1.3",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@types/i2c-bus": "^5.1.0",
    "@types/serialport": "^8.0.1",
    "i2c-bus": "^5.2.0",
    "serialport": "^9.0.1"
  }
}
